---
layout: post
title: "hello, world"
date: 2026-04-07
---

placeholder post — replace before pushing.

this is a sample post showing how a blog entry renders. text, **bold**,
*italic*, `inline code`, [links](/), and code blocks all work out of the box:

```python
def greet(name: str) -> str:
    return f"hello, {name}"

print(greet("world"))
```

> blockquotes look like this — useful for pull-quotes or notes.

- bullet lists
- look like this
- with dotted markers in the soft-terminal style

that's it for the demo.
